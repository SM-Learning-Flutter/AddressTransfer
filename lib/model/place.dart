class Place {
  String title;
  String address;
  String locationName;
  String phoneNum;
  double lat;
  double lng;
  Place(this.title, this.address, this.locationName, this.phoneNum, this.lat,
      this.lng);
}
